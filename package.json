{
  "name": "libya-global-reports",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Libya Global Reports — daily context for decisions in Libya.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview --host 127.0.0.1 --port 4321",
    "check": "astro check",
    "test": "vitest run",
    "collect": "tsx collectors/cli.ts",
    "validate": "tsx scripts/validate-content.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "astro": "^7.3.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
