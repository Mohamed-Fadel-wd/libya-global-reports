Drafts land here after `npm run collect`. They are not published.

Approve by copying verified facts into `content/articles/` or `content/observations/` with `editorialStatus: "published"`.

Live ReliefWeb drafts (`draft-rw-*.json`) are gitignored so the queue does not bulk-commit UN titles. Keep reviewed copies by moving facts into `content/`. Fixture drafts stay tracked.
